# Python practice

Add rewritten solutions under `problems/`, using the topic that best describes
the main technique used to solve the question. Use `snake_case.py` filenames.

Keep data-structure implementations in `data_structures/` and general-purpose
algorithms in `algorithms/`.
